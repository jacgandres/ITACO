@model IEnumerable<ITACO.Model.Venta_Tiene_Producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Producto.NombreProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venta.VentaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaRegistro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.NombreProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.VentaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaRegistro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Venta_Tiene_ProductoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Venta_Tiene_ProductoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Venta_Tiene_ProductoID })
        </td>
    </tr>
}

</table>
