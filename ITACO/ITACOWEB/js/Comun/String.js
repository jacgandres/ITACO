String.prototype.trim = function () { a = this.replace(/^\s+/, ""); return a.replace(/\s+$/, "") }; String.prototype.pad = function (b, d, c) { return d || (d = " "), (b -= this.length) > 0 ? (d = new Array(Math.ceil(b / d.length) + 1).join(d)).substr(0, c = !c ? b : c == 1 ? 0 : Math.ceil(b / 2)) + this + d.substr(0, b - c) : this }; String.prototype.parseBoolean = function () { return (/^true$/i).test(this) };